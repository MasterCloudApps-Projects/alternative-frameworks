package org.acme.dispatcher;

import javax.enterprise.context.ApplicationScoped;
import org.acme.model.StudentMessage;

@ApplicationScoped
public class StudentDispatcher {

    public void dispatch(StudentMessage message) {
        
    }
}
